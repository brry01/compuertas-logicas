/**
 * XOR gate: 
 */

CHIP XOR {
    IN a, b;
    OUT out;

    PARTS:
	NOT(a=a, out=na);
	AND(a=na, b=b, out=and1);
	
	NOT(a=b, out=nb);
	AND(a=a, b=nb, out=and2);
	
	OR(a=and1, b=and2, out=out);
}