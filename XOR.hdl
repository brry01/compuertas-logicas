/**
 * XOR gate: 
 */

CHIP XOR {
    IN a, b;
    OUT out;

    PARTS:
    // Put your code here:
	NOT(a=sel, out=notsel);
	
	AND(a=a, b=notsel, out=and1);
	AND(a=b, b=sel, out=and1);
	
	OR(a=and1, b=and2, out=out);
}